local deltavs is lexicon(
    3, 1000
).

global craft_config is lexicon(
    "circularization_stage", 1,
    "aero_multiplier", 1.05,
    "ascent_steepness", 1,
    "stage_landing_deltav", deltavs
).